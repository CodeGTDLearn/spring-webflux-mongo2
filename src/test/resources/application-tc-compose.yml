# DOCKER-COMPOSE-MODULE(TestContainer)
# HERE, THE PROPERTIES MUST BE USED/DEFINED
# HOWEVER, TRANSACTIONS WILL NOT WORK
# BECAUSE TRANSACTIONS MUST HAVE SPECIFIC PROPERTIES-FILE(MongoDBContainer)

# TEST-TC_COMPOSE
# DEV-DOCKER_COMPOSE
spring:
  data:
    mongodb:
      username: root
      password: root
      authentication-database: admin